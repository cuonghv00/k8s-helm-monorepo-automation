# projects/multi-apps/variables.env
# Literal configurations (ConfigMap)
DB_HOST=postgres.prod.svc.cluster.local
LOG_LEVEL=info
DOMAIN=example.com

# Secrets references (Secret)
DATABASE_PASSWORD=${DATABASE_PASSWORD}
API_TOKEN=${API_TOKEN}
JWT_SECRET=${JWT_SECRET}
